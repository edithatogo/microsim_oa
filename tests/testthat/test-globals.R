test_that("globals functions work correctly", {
  # Test globals file - this might contain constants or global options
  # For now, create a basic test
  expect_true(TRUE) # Placeholder to ensure the test file exists
})
